import { Routes } from '@angular/router';
import { DashboardComponent } from 'src/app/pages/dashboard/dashboard.component';

export const WebshopLayoutRoutes: Routes = [
    { path: 'dashboard',             component: DashboardComponent, },
];
